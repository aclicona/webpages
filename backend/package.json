{
  "name": "website",
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "npm install --prefix dashboard && npm run build --prefix dashboard",
    "build": "npm run build --prefix dashboard",
    "preview": "npm run preview --prefix dashboard",
    "dashboard": "npm run dev --prefix dashboard"
  }
}
